# Google Flow Scripting Skill v1.3.1

Release date: 2026-07-23

## Validator

- Scoped beat extraction to each segment's `VIDEO PROMPT` region so storyboard time ranges did not contaminate the video timeline.
- Fell back to whole-segment scanning only when no `VIDEO PROMPT` marker existed.
- Preserved raw beat order and duplicate detection within the scoped region.
- Improved hex-colour detection while allowing numeric issue references such as `#123` and `#1234`.

## Tests and installation

- Added storyboard-panel and storyboard/video-timeline regression fixtures.
- Raised the suite to 24 checks.
- Documented `--force` for non-interactive reinstalls while retaining automatic backups.
